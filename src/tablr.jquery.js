"use strict";

(function(w, $)
{
    w.Tablr = function(/* Object */ options)
    {
        this.options = $.extend({}, options);
        
        // TODO
    };

    w.Tablr.prototype = {
        // TODO
    };
})(window, jQuery);